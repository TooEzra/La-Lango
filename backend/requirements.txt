# Core dependencies for La Lango AI
# Install with: pip install -r requirements.txt

# Web framework — used to build the translation API
fastapi==0.111.0

# ASGI server — needed to run FastAPI
uvicorn==0.30.1

# Data validation — FastAPI uses this for request/response schemas
pydantic==2.7.1

# Numerical computing — used in evaluation metrics and data processing
numpy==1.26.4

# Progress bars — makes training loops easier to follow
tqdm==4.66.4

# PyTorch is the recommended framework for the reference implementation
# Other frameworks (TensorFlow, JAX) are also welcome — see docs/
torch==2.3.0

# For loading and saving model configurations
pyyaml==6.0.1
